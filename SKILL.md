---
name: math-modeling-framework-diagram
description: 从赛题、解题方案或论文草稿创建、修改、自动排版、渲染并检查论文级数学建模框架图、技术路线图、求解流程图和模型关系图，并在有论文原文时审核节点、关系和结论是否一致；当用户要求可编辑或矢量成图时也使用。不要用于普通统计图或数据可视化。
---

# 数学建模框架图

把自然语言要求、赛题或论文方案变成结构清晰、版式克制、可编辑且经过检查的论文级框架图。“给这道题画论文框架图”已经是充分输入。

正式成图前必须完整阅读 [论文级视觉系统](references/visual-system.md)。需要创建或修改 JSON 规格时，阅读 [可执行规格与命令](references/diagram-spec.md)。用户提供了论文、赛题原文或解题文档时，还必须完整阅读 [论文原文一致性自检](references/logic-audit.md)。

## 默认执行路线

对 `pipeline`、`comparison`、`swimlane`、`hub`、`hierarchy` 五种布局，优先使用本技能自带的确定性工具包：

1. 从用户材料提取节点、关系、控制条件、共享输入、评价指标和结论。
2. 按 [diagram-spec.schema.json](references/diagram-spec.schema.json) 创建 UTF-8 JSON 规格。JSON 是唯一规范源；不要直接手写 SVG 或 draw.io XML。
3. 先严格检查规格：

   ```powershell
   python "<技能目录>\scripts\diagram_toolkit.py" validate --spec "<规格.json>" --strict
   ```

4. 检查通过后一次生成全部交付物：

   ```powershell
   python "<技能目录>\scripts\diagram_toolkit.py" build --spec "<规格.json>" --output-dir "<输出目录>" --name "<文件主名>" --strict
   ```

5. 有论文、赛题原文或解题文档时，执行“证据检索—AI 语义审核—审核验证”。检查节点、箭头、顺序、公式参数、结论、遗漏和臆造；审核不通过就修改 JSON 并重新构建。
6. 打开并实际查看 PNG 或 SVG。自动报告通过不等于视觉复检完成；发现拥挤、交叉、错误分级或阅读顺序不清时，修改 JSON 后重新构建。
7. 默认最多迭代三轮；三轮后仍不适合自动布局或原文含义无法确认时，保留全部报告并向用户说明具体争议，不得自行把不确定内容判为一致。

`<技能目录>` 指当前 `SKILL.md` 所在目录。脚本默认生成 `.spec.json`、`.svg`、`.png`、原生可编辑 `.drawio`、`.mmd` 和 `.quality-report.json`。同名文件已存在时自动创建 `_v2`；只有用户明确要求替换时才添加 `--overwrite`。

## 布局选择

- `pipeline`：顺序明确的建模或求解流程。
- `comparison`：统一条件下的方法比较、选参、实验、评价和结论。
- `swimlane`：一题多问、并行方案或共享数据下的多个任务。
- `hub`：一个核心模型向参数、求解、检验和应用展开。
- `hierarchy`：指标体系、依赖树或中等规模有向无环图。

约 6–16 个普通节点优先使用工具包。节点超过 16 个、边明显多于节点、存在大量交叉或需要聚类布局时，改用 D2 或 Graphviz，并仍按视觉系统检查。公式密集且用户采用 LaTeX 工作流时，可改用 D2 LaTeX 标签或 TikZ。只有用户明确要求白板或在线协作风格时，才使用 Excalidraw 或 ProcessOn。

## 语义与视觉约束

- 保留用户明确给出的题号、模型、算法、变量、公式、因果关系和前后依赖。
- 用户只提供赛题时可以提出候选模型，但必须标明关键假设，不能把推测冒充既有结论。
- 控制条件放入 `control`，总结放入 `result`；不要把它们伪装成普通步骤卡片。
- 一个节点只表达一个动作或判断；节点 ID 必须稳定、有意义且只用英文字母、数字、下划线或连字符。
- 正文以 3–5 个短项为宜；先压缩信息，再增加节点。
- draw.io 必须包含独立可编辑节点和边，不能把整张 SVG 嵌入后冒充可编辑文件。

## 论文原文一致性自检

只要用户提供了可读取的原文，自检就是默认交付步骤，无需用户再次提出。精确命令和审核 JSON 结构见 [论文原文一致性自检](references/logic-audit.md)。

- 程序层负责从 TXT、Markdown、LaTeX、DOCX、JSON 或 PDF 中定位证据，并把证据包绑定到当前原文和当前规格的 SHA-256 摘要。
- AI 层必须亲自阅读相关原文，逐项判断 `control`、每个节点、每条边和 `result`，不能照抄关键词分数。
- 必查六项：内容覆盖、术语与公式、方向与因果、步骤顺序、重要遗漏、无依据添加。
- AI 语义审核必须包含短原文摘录和页码、段落或行号；证据摘录每条不超过 220 字。
- `logic_audit.py verify` 通过后才可声称“与论文原文一致”。如果没有原文、PDF 无文字层且未完成 OCR、或原文自身矛盾，结论必须是 `unverifiable`，不能猜测。

## 验收与失败处理

交付前同时满足：

- `*.quality-report.json` 的 `passed` 为 `true`；
- 有原文时，`*.logic-check.json` 的 `passed` 也为 `true`；
- PNG/SVG 已被实际查看，标题、正文、公式、箭头和分组没有裁切、重叠或乱码；
- 主路径数秒内可辨认，目标论文尺寸下仍可读；
- draw.io 可以打开，且可编辑节点数不少于规格节点数；
- 规范源、预览图和可编辑文件的内容一致。
- 结论、模型、参数、公式和箭头均有原文证据，没有未解释的遗漏或臆造。

PNG 渲染依次尝试 CairoSVG、Pillow 和本机 Chrome/Edge；Codex 自带图像运行时可作为 Pillow 后备。若环境没有渲染器，仍交付 SVG、draw.io、Mermaid 和规格，并明确说明 PNG 缺失。网站不可用、需要登录、出现验证码或阻止导出时，立即回到本地工具包；不得因此丢失规范源。

## 文件与权限

- 使用与题目相关的文件名，例如 `problem_2_potential_comparison_framework`。
- 不覆盖无关文件；不把临时 HTML 或浏览器配置放入用户交付目录。
- 未经授权，不登录网站、不公开发布、不上传私人赛题、不创建共享链接，也不消耗付费额度。
- GitHub 默认只读；用户未明确要求时，不创建仓库、提交、议题或拉取请求。

## 调用示例

- “用这道赛题生成一张论文用求解框架图。”
- “把问题一到问题四画成相互关联的技术路线图，保留核心公式。”
- “把现有框架图改成 A4 横版期刊风，并给我 draw.io、SVG 和 PNG。”
